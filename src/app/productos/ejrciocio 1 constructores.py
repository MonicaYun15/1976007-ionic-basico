class Persona:
    def __init__(self, nombre = None, edad = None, direccion = None):
        self.nombre = nombre
        self.edad = edad
        self.direccion= direccion
def imprimir(self):
    if self.nombre is not None and self.edad is not None and self.direccion is not None:
        print(f"nombre(self.nombre)")
        print(f"edad(self.edad)")
        print(f"direccion(self.dirrecion)")
    else:
        print("datos incomplretos")

